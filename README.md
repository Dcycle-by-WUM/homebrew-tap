# homebrew-tap

Homebrew tap for `dc-walrus`.

Install:

```bash
brew tap Dcycle-by-WUM/tap
brew install dc-walrus
```

If the formula has already been tapped explicitly:

```bash
brew install Dcycle-by-WUM/tap/dc-walrus
```
